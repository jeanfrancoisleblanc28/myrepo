export const metadata = {
  title: "Présentation UI/UX — MyRepo",
  description: "Mode présentation plein-écran pour ton kit UI/UX.",
};

export default function PresentLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
